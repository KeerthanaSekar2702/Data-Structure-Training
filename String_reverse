#include <stdio.h>
#include <string.h>
int main() {
    char brand[] = "MSI";
    int len = strlen(brand);
    
    printf("Reversed string: ");
    for (int i = len - 1; i >= 0; i--) {
        printf("%c", brand[i]);
    }
    printf("\n");
    return 0;
}
